# SIEM VM for AI-Powered Attack Prediction

This VM hosts all components required for real‑time network monitoring, LSTM‑based attack prediction, and log analysis using the ELK stack.

# Access the services:
-    LSTM API: http://localhost:8001/docs
-    Backend API: http://localhost:8000/docs
-    Dashboard: http://localhost:8501
-    Kibana: http://localhost:5601
-    MailHog (if used) – not included here; see App Server.

